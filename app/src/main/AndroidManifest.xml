<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chiranths.jobportal1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/dataflair"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dataflair"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobPortal">
        <activity
            android:name=".Activities.Propertys.PropertyActivity"
            android:exported="false" />
        <activity android:name=".Activities.jobs.JobDetailsActivity" />
        <activity android:name=".Activities.jobs.AdminActivity" />
        <activity android:name=".Activities.jobs.RoleActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.AdminAddNewProductActivity"/>
        <activity android:name=".Activities.jobs.StartingActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>